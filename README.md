
# Nuxt 3 Portfolio

## Setup
```bash
npm install
npm run dev
```

## Customize
- Update links in `components/Projects.vue` and `components/Contact.vue`
- Replace placeholder image in `Hero.vue`
- Deploy to Netlify/Vercel or `npm run generate` for static export
